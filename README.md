# ahlan

Simple static site: **`/app`** detects Android / iOS / other and redirects to Play Store, App Store, or the Ahlan Reward web page.

Deploy on [Vercel](https://vercel.com) from this repo. Your link will be `https://<project>.vercel.app/app` (or `https://yourdomain.com/app` with a custom domain).
